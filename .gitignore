# Godot 4+ specific ignores
.godot/
/android/

# 导出的游戏包
exported/
export_presets.cfg.bak  # 导出预设的备份文件

# 临时文件/缓存
*.tmp
*.godot#  # Godot的临时备份文件
*.import# # 导入的临时备份文件
godot.log # Godot的日志文件
.export_template_cache/ # 导出模板缓存

# 系统文件
.DS_Store       # Mac系统
Thumbs.db       # Windows系统
desktop.ini     # Windows系统

# 编辑器配置（可选：如果团队使用不同编辑器，忽略；否则可提交）
.vscode/        # VSCode配置
.idea/          # JetBrains IDE配置
*.swp           # Vim临时文件

.history/